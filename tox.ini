[tox]
envlist = py{37,38,39,310}-{linux,macos,windows}

requires =
    setuptools

[testenv]
deps =
    faker
    pytest
commands =
    pytest

[testenv:dev]
basepython = python3.9
deps =
    faker
    pytest
    pytest-cov
commands =
    coverage erase
    pasteurize -w "valvefgd"
    pytest --cov=valvefgd --cov-report html

[testenv:coverage]
basepython = python3.9
deps =
    faker
    pytest
    pytest-cov
    coverage-badge
commands =
    coverage erase
    pasteurize -w "valvefgd"
    pytest --cov=valvefgd --cov-report html
    coverage-badge -fo docs/source/coverage.svg

[testenv:docs]
basepython = python3.9
deps =
    sphinx
    faker
    pytest
    pytest-cov
    coverage-badge
commands =
    coverage erase
    pasteurize -w "valvefgd"
    pytest --cov=valvefgd --cov-report html
    coverage-badge -fo docs/source/coverage.svg
    python -m sphinx docs/source/ docs/build/